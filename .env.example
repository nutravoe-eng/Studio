# Sanity Studio — copy to .env and fill in.
# Used only by seed/migration scripts. Studio UI uses `npx sanity login`.
# Create a token at https://www.sanity.io/manage → Project → API → Tokens
# Never commit .env.
SANITY_TOKEN=
